(ns fif.stash
  "Functions for manipulating stashes, not to be confused with stacks.
  A stash in this case is a sequence containing stowaway information
  that will be refered to later on."
  (:require
   [fif.stack :as stack]))

